server:
  port: 8870
  servlet:
    context-path: /access

spring:
  profiles:
#    active: dev
    active: test
  application:
    name: ${APP_ID:iot-gateway-other}
#  main:
#    allow-circular-references: true
  cloud:
    nacos:
      config:
        isUseCloudNamespaceParsing: false
        server-addr: ${NACOS_ADDR:10.39.68.135:8804}
        namespace: enn-${spring.profiles.active}-public
        file-extension: yaml

skywalking:
  agent:
    service_name: ${spring.application.name}
