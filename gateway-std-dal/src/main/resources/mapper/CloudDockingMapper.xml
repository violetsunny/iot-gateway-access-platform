<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ennew.iot.gateway.dal.mapper.CloudDockingMapper">

    <resultMap id="BaseResultMap" type="com.ennew.iot.gateway.dal.entity.CloudDockingEntity">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="code" column="code"/>
        <result property="baseUrl" column="base_url"/>
        <result property="createTime" column="create_time"/>
        <result property="state" column="state"/>
    </resultMap>
    <select id="queryPage" parameterType="Map"
            resultMap="BaseResultMap">
        select * from cloud_docking_base
        <where>
            <if test="params.id != null and params.id != ''">
                and id = #{params.id}
            </if>
            <if test="params.name != null and params.name != ''">
                and name like concat('%',#{params.name},'%')
            </if>
            <if test="params.code != null and params.code != ''">
                and code = #{params.code}
            </if>
            <if test="params.state != null">
                and state = #{params.state}
            </if>
        </where>
    </select>

    <select id="queryById" resultMap="BaseResultMap">
        select * from cloud_docking_base
        where id = #{id}
    </select>
</mapper>
